## 余智护杭任务同步分析 tab 优化（2026-05-28）

### 表2 多项改进
- **默认排序**：从 village/asc 改为 rate/desc，完成百分比从高到低
- **序号列**：最左侧新增 `#` 列，自动编号，合计行留空
- **多选筛选**：文本搜索 → 下拉多选（checkbox），50 个村社可多选筛选
- **确认延迟生效**：引入 `draftSelected` 临时 state，勾选不立即刷新表格，点"确定"才应用
- **筛选后合计动态变化**：totalRow 改为 useMemo 动态计算当前可见数据的合计
- **面板固定定位**：position:fixed + getBoundingClientRect() 计算坐标，解决右侧裁剪问题
- **按钮栏固定**：flex 布局 + flexShrink:0，顶部"清除/全选/确定"始终可见，选项列表独立滚动
- **外部点击关闭**：useRef + useEffect mousedown 监听替代全屏遮罩

### 表1 数据（初始创建）
- 14项已同步任务分析：正常同步 5238 + 数据校验异常 8627 = 总计 13865
- 绿色/红色背景区分，小计行/总计行高亮

### 表1 后续迭代
- **调换表顺序**：村社检查任务统计 → 表1，14项已同步任务分析 → 表2
- **拆分子列**：任务数 → 防灾减灾/日常检查/141同步 三类，每类含任务数/已完成/完成率/隐患数/已整改/整改中 6 子列
- **完成率**：仅百分比文字，移除进度条
- **列偏移修复**：通过 borderRight 替代空 `<td>` 分隔列
- **合计行置顶**：position:sticky 固定在表头下方
- **分页**：每页 10 条，含完整分页器（首页/上一页/页码/下一页/末页）
- **日期筛选**：dataFrom/dataTo 输入框 + 清除时间按钮
- **标签改名**：数据日期 → 任务创建时间
- **筛选栏靠右**：justifyContent: 'flex-end'
- **快速筛选**：本月/上月/本季/本年 四个按钮，点击自动填充日期范围，选中高亮，手动修改日期时清除选中状态
- **导出明细**：表2标题栏右侧新增导出按钮，导出 CSV（状态/任务去向/异常信息/任务数/占比），排除小计/总计行，带 BOM 支持 Excel 中文
- **指标统计看板**：表1上方新增三卡片看板（企业数/场所数/出租房数），mock 数据，后续替换真实接口
