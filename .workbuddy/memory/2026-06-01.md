# 2026-06-01

## 村社数据看板 - 筛选条件全局化
- 将表1内的村社筛选条件（时间筛选 + 快速筛选 + 村社多选下拉）提升为村社数据看板的全局筛选栏
- 筛选栏放在指标统计看板卡片之上，作为整个 YuzhiSyncDimension 组件的顶部筛选
- 统计分析区域改为基于筛选后的数据（`filteredVillages` 而非 `allVillages`），村社数量和统计汇总均随筛选联动
- `overallStats` 改用 `filteredVillages` 计算，统计分析文案中的村社数量同步更新
- 构建验证通过，无新增 TypeScript 错误
