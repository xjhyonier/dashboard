# 2026-06-04 工作日志

## HazardDimension - hazard看板筛选条件精简
- 删除"所属行业"和"负责工作组"两个筛选器 UI（已在全局筛选里有了）
- 关键词搜索 placeholder 从"责任主体名称 / 隐患描述 / 检查人员"改为"隐患描述"
- state/useState 逻辑保留（不影响功能，无TypeScript报错）
- commit: 276f8bc（本地已提交，需手动 git push xjh master 推送）

## SpecialDimension - 任务计划tab改动
- TaskTab 类型扩展为包含 '全部'，默认激活'全部'
- '全部'tab 时显示 filteredTasks 全部数据，其他tab按type过滤
- 删除导出按钮及 exportToCSV import
- 搜索框靠右（tab左，搜索框右，中间无导出按钮）
- commit: 84ca61b（本地已提交，需手动 git push xjh master 推送）

## generator.ts - 日常检查任务状态修复
- 两处日常检查任务的 `status: '进行中'` 改为根据 endDate 动态判断
- 系统生成的按风险等级任务：`new Date() > new Date(endDate) ? '已完成' : '进行中'`
- 手动创建的按月任务：同样逻辑，endDate 为当年对应月30日
- 现在日历月份已过的任务（如五一节前安全检查 month:4，当前已是6月）会显示为"已完成"
- commit: 5f8a845（本地已提交，需手动 git push xjh master 推送）

## HazardDimension - 责任主体维度隐患统计删除检查人员字段
- 删除 stats 类型定义中的 `expertName: string`
- 删除 stats 初始化中的 `expertName: h.expert_name`
- 删除表头列 `{ key: 'expertName', label: '检查人员', sortable: true }`
- 删除数据行 `<td>{row.expertName}</td>`
- colSpan 10→9
- 导出数据删除 `检查人员: row.expertName,` 和 `{ key: '检查人员', label: '检查人员' }`
- commit: 6208828（本地已提交，需手动 git push xjh master 推送）

## TrendCharts - 趋势分析三张表标题加「近一年」前缀
- "隐患数量趋势" → "近一年隐患数量趋势"
- "单位每月自查自纠统计" → "近一年单位每月自查自纠统计"
- "检查人员数据统计" → "近一年检查人员数据统计"
- commit: f98a7b0（本地已提交，需手动 git push xjh master 推送）

## StateDimension - 责任主体列表搜索框placeholder精简
- "搜索责任主体名称 / 工作组 / 行业" → "搜索责任主体名称"
- commit: bfaa0ca（本地已提交，需手动 git push xjh master 推送）

## StationChiefV2Dashboard - 隐患总数/重大隐患总数增加月环比和月同比
- kpiTotals 新增上月同期(prevMonth)和去年同月(lastYear)统计
- 新增 `calcChange()` 计算环比同比百分比
- KpiCard 组件扩展 `mom`/`yoy` props，formatChange/changeColor/changeArrow 辅助函数
- 正值为红色(↑)，负值为绿色(↓)，0为灰色(→)
- 显示格式: `↑+12.5% 环比 | ↓-3.2% 同比`
- 仅隐患总数和重大隐患总数两张卡片传入对比数据
- commit: 98b65a1（本地已提交，需手动 git push xjh master 推送）
