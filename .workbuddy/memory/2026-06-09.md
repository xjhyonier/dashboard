## 2026-06-09

### 时间筛选调整（StationChiefV2Dashboard）
- 删除「自定义」时间筛选及对应的 customStart/customEnd state 和 month input
- 新增「上月」(prevMonth) 和「上季」(prevQuarter) 时间维度
- 按钮顺序：本月 → 上月 → 本季 → 上季 → 本年
- 在模块顶部计算 prevMonthStart/End、prevQuarterStart/End 常量
- 同步更新 types.ts 中 TimeRange 类型定义（加入 prevMonth | prevQuarter）
- 编译通过，commit: f247823

### 任务计划tab删除「全部任务-汇总进度」区块
- 从 SpecialDimension.tsx 删除 JSX 区块及 taskProgressByOrg/ByPerson 两个 useMemo 死代码
- 该表已移至组织与人员tab DutyDimension.tsx 表4上方，不受影响
- commit: f8cd827, 2f1706c

### 隐患明细列表删除「行业」字段
- HazardDimension.tsx：删除表头 SortableTh、数据单元格、导出映射/列定义、筛选逻辑、state、industries useMemo、重置按钮调用
- colspan 从 11 调整为 10
- 编译通过，commit: 4fee6bb

### 全局筛选删除「全部企业」「全部行业」
- StationChiefV2Dashboard.tsx：删除企业筛选 select（line 463-484）、行业筛选 select（line 486-505）
- 重置按钮条件/onClick 移除对应引用
- 工作组 onChange 中移除 setFilterEnterprise('all')
- state 和 TrendCharts 内部过滤逻辑保留（状态始终为'all'不影响）
- 编译通过，commit: 5a82443

### 标签隐患分析统计表-编辑标签选项更新
- IndustryDimension.tsx：
  - 删除原动态 tagLibrary（从 hazards.enterprise_industry 聚合）
  - 新增 `tagGroups` useMemo：固定6个一级分类及对应二级标签（用户提供数据）
  - 新增 `allTags` useMemo：扁平化所有"一级：二级标签"格式字符串
  - 新增 `filteredTagGroups` useMemo：按搜索关键词过滤分组标签
  - 标签选择浮层 UI 改为按一级分类分组渲染，`selectedTags` 存储"一级：二级标签"格式
  - 修改 `filtered` useMemo 过滤逻辑：从 selectedTags 中提取二级标签匹配 d.industry
- 编译通过，commit: 9c3064b
