## 2026-06-10

### 标签隐患分析统计表 - 标签选择浮层交互优化
- IndustryDimension.tsx：
  - 添加 `expandedCategories` 状态，记录展开的一级分类
  - 一级分类标题可点击，带 ▶/▼ 箭头，显示标签数量
  - 默认全部折叠，点击展开对应二级标签
  - 二级标签只显示二级名称（不显示"一级：二级"前缀）
  - 已选标签也只显示二级名称
- 编译通过，commit: 575b426

### 已知问题
- 当前过滤逻辑只按 `enterprise_industry` 聚合，只有"企业行业类别"标签能正确过滤
- 其他分类标签（风险等级、消防类型等）选中后不会触发过滤
- 如需支持多维度过滤，需重新设计 `hazardAnalysis` 聚合逻辑

## 新增企业使用数据看板 (15:11)

### 需求
用户要求在"镇街数据看板"平级再增加一个"企业使用数据看板"，包含7个tab：总览、待办、制度台账、教育培训、现场管理、双重预防、入驻单位管理。

### 实现内容
1. **角色定义** (`src/types/role.ts`):
   - 在 `emergencyConfig.roles` 中新增 `enterprise-dashboard` 角色
   - name: '企业使用数据看板', title: '企业使用数据看板'

2. **页面组件** (`src/business/emergency/pages/EnterpriseDashboard.tsx`):
   - 创建新文件，包含7个tab的切换功能
   - 当前各tab为占位内容（PlaceholderTab），后续逐步完善

3. **路由配置** (`src/App.tsx`):
   - 导入 `EnterpriseDashboard` 组件
   - 在 `ROLE_PATHS.emergency` 中添加 `'enterprise-dashboard': '/emergency/enterprise-dashboard'`
   - 添加 `<Route path="/emergency/enterprise-dashboard" element={<EnterpriseDashboard />} />`

### 提交信息
- commit: `c92fe80` - "feat: 新增企业使用数据看板角色和页面框架"
- 已推送到 `xjh master`

### 后续工作
- 各tab的具体内容需要逐步完善（总览、待办、制度台账、教育培训、现场管理、双重预防、入驻单位管理）

## 修复制度台账Tab内容未显示 (16:44)
- 问题：制度台账Tab的渲染逻辑仍指向 PlaceholderTab，未绑定 SystemTabContent 组件
- 修复：将 `activeTab === 'system'` 的渲染从 `<PlaceholderTab title="制度台账" />` 改为 `<SystemTabContent />`
- 制度台账Tab现在包含：3个指标卡、生产企业统计表、消防场所统计表、制度台账明细表
- Commit: d1b7c29

## 制度台账交叉表改为按已完善户数统计 (17:35)
- 用户要求：生产企业和消防场所统计表每列按已完善企业户数展示
- 数据从独立的随机数据改为从 enterpriseDetails 分组统计
- 新增 productionCrossTable（按风险等级）和 fireCrossTable（按消防类型）
- 表格展示从 ✓/✗ 改为 "已完善户数/总户数" 格式
- Commit: cb8545b

## 制度台账统计表增加总户数列、数值改纯数字 (17:48)
- '是否全部完善' 列名改为 '全部完善户数'
- 左侧新增 '总户数' 列
- 后7列数值改为纯数字（不显示分母）
- Commit: 8ccd70c
