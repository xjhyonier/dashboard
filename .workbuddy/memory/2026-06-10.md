## 2026-06-10

### 标签隐患分析统计表 - 标签选择浮层交互优化
- IndustryDimension.tsx：
  - 添加 `expandedCategories` 状态，记录展开的一级分类
  - 一级分类标题可点击，带 ▶/▼ 箭头，显示标签数量
  - 默认全部折叠，点击展开对应二级标签
  - 二级标签只显示二级名称（不显示"一级：二级"前缀）
  - 已选标签也只显示二级名称
- 编译通过，commit: 575b426

### 已知问题
- 当前过滤逻辑只按 `enterprise_industry` 聚合，只有"企业行业类别"标签能正确过滤
- 其他分类标签（风险等级、消防类型等）选中后不会触发过滤
- 如需支持多维度过滤，需重新设计 `hazardAnalysis` 聚合逻辑
