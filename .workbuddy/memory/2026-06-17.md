## 2026-06-17

## 村社数据看板折线图优化 (13:23)
### 修改内容：
1. **标题修改**：从"村社近一年每月检查任务数据"改为"村社近期检查数据变化"
2. **新增时间维度切换**：
   - 支持"近12个月"、"近12周"和"近30天"三种维度切换
   - 添加切换按钮组（紫色主题色）
   - 默认显示"近12个月"视图

### 技术实现：
- 新增 `timeDimension` 状态变量（`'12months'` | `'12weeks'` | `'30days'`）
- 新增 `generateDailyTrendData` 函数生成近30天的每日趋势数据
- 新增 `generateWeeklyTrendData` 函数生成近12周的每周趋势数据
- 修改 `generateMonthlyTrendData` 函数返回 `TrendDataPoint` 接口（使用 `period` 字段代替 `month`）
- X轴根据维度动态调整显示间隔（近30天时每5天显示一个刻度，近12周时每2周显示一个刻度）
- 文件：`YuzhiSyncDimension.tsx`
- Commit: 5bad1ab
