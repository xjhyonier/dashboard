## 2026-06-22

## 村社数据看板-折线图数据维度修改 (10:50)
### 修改内容：
将"近12周"和"近30天"的数据维度从相对值改为绝对值：

**修改前（近12周、近30天）**：
- 任务总数、完成率、查出隐患数、隐患整改完成率

**修改后（近12周、近30天）**：
- 完成任务数、发现隐患数、整改隐患数

**近12个月保持原结构**：
- 任务总数、完成率、查出隐患数、隐患整改完成率（带双Y轴）

### 技术实现：
- 更新 `TrendDataPoint` 接口：新增3个绝对数值字段（完成任务数、发现隐患数、整改隐患数），原4个字段改为可选
- 重写 `generateWeeklyTrendData` 函数：生成每周的完成任务数、发现隐患数、整改隐患数
- 重写 `generateDailyTrendData` 函数：生成每日的完成任务数、发现隐患数、整改隐患数
- 图表渲染逻辑：
  - 近12个月：使用双Y轴（左轴绝对数、右轴百分比），显示4条折线
  - 近12周、近30天：使用单Y轴（绝对数值），显示3条折线
  - 图例根据维度动态切换
- 删除不再使用的 `CustomLegend` 组件（改为内联图例）
- 文件：`YuzhiSyncDimension.tsx`
- Commit: 1d14a14

## 任务统计指标卡修改 (12:35)
### 修改内容：
将任务统计指标卡从3个指标改为7个指标：

**新指标（统计昨日和上周数据）**：
- 新增任务数：查询期间新增的任务数
- 完成任务数：查询期间完成的任务数
- 未完成任务数：截止查询时间，未完成的任务数
- 累计完成率：截止查询时间，总完成任务数/总任务数
- 发现隐患数：查询期间发现的隐患数
- 整改隐患数：查询期间整改的隐患数
- 累计整改率：截止查询时间，总整改隐患数/总隐患数

### 技术实现：
- 添加 `taskStatMetrics` 数组：定义7个指标及其口径解释
- 添加 `generateTaskStats` 函数：生成昨日和上周的模拟数据
- 添加 `hoveredMetric` 状态：控制悬浮备注弹窗显示
- 重新设计指标卡布局：
  - 顶部：图标 + 标题 + 备注图标（?），悬浮显示所有指标解释
  - 底部：指标表格（3列：指标、昨日、上周），显示7个指标的数值
- 修复语法错误：
  - `cards` 数组缺少对象开括号 `{`
  - 添加 `React` 默认导入（用于 `React.Fragment`）
- 文件：`YuzhiSyncDimension.tsx`
- Commit: 201293e

## 任务统计指标卡修改 (13:08)
### 修改内容：
- 指标名称：未完成任务数 → 累计未完成任务数
- 备注说明：截止查询时间，未完成的任务数 → 截止查询时间，累计未完成的任务总数
- 文件：`YuzhiSyncDimension.tsx` (第725行)
- Commit: 1a37edb

## 村社近期检查数据变化-增加新增任务数维度 (13:11)
### 修改内容：
为"近12周"和"近30天"维度增加"新增任务数"指标

### 技术实现：
1. **TrendDataPoint接口**：添加 `新增任务数?` 字段（第164行）
2. **generateWeeklyTrendData()**：
   - 添加 `baseNewTasks` 基准值（每周新增任务数，略多于完成任务数）
   - 在循环中计算 `newTasks` 并添加到返回对象
3. **generateDailyTrendData()**：
   - 添加 `baseNewTasks` 基准值（每日新增任务数）
   - 在循环中计算 `newTasks` 并添加到返回对象
4. **图表渲染逻辑**：
   - 更新图例：添加"新增任务数"（颜色：#7C3AED）
   - 添加第4条Line：`<Line dataKey="新增任务数" stroke="#7C3AED" />`
   - 现在近12周和近30天都显示4条线：新增任务数、完成任务数、发现隐患数、整改隐患数
- 文件：`YuzhiSyncDimension.tsx`
- Commit: 354d374

## 村社近期检查数据变化-折线图显示数值标签 (13:24)
### 修改内容：
为"村社近期检查数据变化"图表的所有折线添加数值标签

### 技术实现：
1. **导入LabelList**：从recharts导入LabelList组件（第3行）
2. **近12个月图表**（4条线）：
   - 任务总数：添加LabelList，位置top，颜色#4F46E5
   - 完成率：添加LabelList，formatter添加"%"后缀，颜色#059669
   - 查出隐患数：添加LabelList，位置top，颜色#DC2626
   - 隐患整改完成率：添加LabelList，formatter添加"%"后缀，颜色#D97706
3. **近12周/近30天图表**（4条线）：
   - 新增任务数：添加LabelList，位置top，颜色#7C3AED
   - 完成任务数：添加LabelList，位置top，颜色#4F46E5
   - 发现隐患数：添加LabelList，位置top，颜色#DC2626
   - 整改隐患数：添加LabelList，位置top，颜色#059669
4. **样式统一**：字体大小9px，位置均为"top"（在数据点上方显示）
- 文件：`YuzhiSyncDimension.tsx`
- Commit: fb7b803
- 变更：25行新增，9行删除

## 表1村社检查任务统计-修改为9个指标 (13:55)
### 修改内容：
将表1的列从7个指标改为9个指标，并添加指标说明悬浮框

### 9个新指标：
1. 总任务数：截止查询时间，共有多少任务数
2. 总完成率：截止查询时间，总已完成任务数/总任务数
3. 新增任务数：查询期间新增的任务数
4. 新增完成数：查询期间完成的任务数
5. 总隐患数：截止查询时间，共有多少隐患数
6. 整改完成率：截止查询时间，已整改的隐患数/总隐患数
7. 确认隐患数：查询期间新增的隐患数
8. 已整改：查询期间已整改的隐患数
9. 整改中：查询期间整改中的隐患数

### 技术实现：
1. **TaskSub接口**：添加3个新字段
   - `newTasks: number` - 新增任务数
   - `newDone: number` - 新增完成数
   - `newHazard: number` - 确认隐患数（查询期间新增）
2. **genMock函数**：生成模拟数据
   - 新增任务数 = total × (10-20%)
   - 新增完成数 = done × (10-20%)
   - 确认隐患数 = hazard × (10-20%)
3. **表头修改**：
   - 添加`showTableHelp`状态控制悬浮框
   - 在标题右侧添加"?"图标，悬浮显示9个指标的解释
   - 修改分组表头`colSpan`从7改为9
   - 修改第二级表头，显示9个指标名称
4. **renderSubCols函数**：渲染9个指标的数据
   - 使用不同颜色区分：紫色(新增任务)、蓝色(新增完成)、红色(总隐患)、橙色(确认隐患、整改中)、绿色(已整改)
5. **排序功能**：更新`SortCol`类型和`getSortValue`函数，支持新字段排序
6. **汇总统计**：更新`overallStats`计算，包含新字段
- 文件：`YuzhiSyncDimension.tsx`
- Commit: 8198762
- 变更：159行新增，44行删除

## 表1指标卡-修改指标名称 (14:08)
### 修改内容：
将表1顶部的4个指标卡中的指标名称改为与表1列名一致

### 修改前后对比：
| 位置 | 修改前 | 修改后 |
|------|--------|--------|
| 第1个指标 | 任务总数 | 总任务数 |
| 第2个指标 | 完成率 | 总完成率 |
| 第3个指标 | 查出隐患数 | 总隐患数 |
| 第4个指标 | 隐患整改完成率 | 整改完成率 |

### 技术实现：
- 修改4个指标卡的标签文本（第1323-1330行）
- 保持数据计算逻辑不变（仍使用overallStats汇总数据）
- 文件：`YuzhiSyncDimension.tsx`
- Commit: 092a451

## 完成任务数改为新增完成数 (14:10)
### 修改内容：
将"完成任务数"改为"新增完成数"，涉及两处：
1. 任务统计指标卡
2. 村社近期检查数据变化图表（近12周/近30天）

### 技术实现：
1. **TrendDataPoint接口**（第163行）：`完成任务数?` → `新增完成数?`
2. **generateWeeklyTrendData()**（第296、306行）：字段名改为`新增完成数`
3. **generateDailyTrendData()**（第245、255行）：字段名改为`新增完成数`
4. **图表图例**（第1070行）：`完成任务数` → `新增完成数`
5. **Line组件**（第1075行）：`dataKey="完成任务数"` → `dataKey="新增完成数"`
6. **LabelList组件**（第1076行）：`dataKey="完成任务数"` → `dataKey="新增完成数"`
7. **任务统计指标卡**（第726行）：`name: '完成任务数'` → `name: '新增完成数'`
- 文件：`YuzhiSyncDimension.tsx`
- Commit: 3cc1fc3
- 变更：7行修改

## 任务统计指标卡-修改指标名称与备注说明 (14:14)
### 修改内容：
将任务统计指标卡的指标名称与备注说明修改为与表1：村社检查任务统计保持一致

### 修改前后对比：
| 位置 | 修改前 | 修改后 | 备注说明（修改后） |
|------|--------|--------|-------------------|
| 第4个指标 | 累计完成率 | 总完成率 | 截止查询时间，总已完成任务数/总任务数 |
| 第5个指标 | 发现隐患数 | 确认隐患数 | 查询期间新增的隐患数 |
| 第6个指标 | 整改隐患数 | 已整改隐患数 | 查询期间已整改的隐患数 |
| 第7个指标 | 累计整改率 | 整改完成率 | 截止查询时间，已整改的隐患数/总隐患数 |

### 技术实现：
- 修改`taskStatMetrics`数组（第792-800行）：
  - 更新指标名称（`name`字段）
  - 更新备注说明（`definition`字段）与表1的悬浮备注保持一致
- 表格渲染逻辑自动更新（通过`taskStatMetrics.map()`动态渲染）
- `isRate`判断逻辑保持不变（检查名称是否包含"率"字）
- 文件：`YuzhiSyncDimension.tsx`
- Commit: 151c50a
- 变更：4行修改
