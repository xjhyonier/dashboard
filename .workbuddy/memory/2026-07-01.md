# 2026-07-01

## 修改: 任务计划tab - 删除状态列、新增已整改列、添加修改记录弹窗

### 数据层变更
- `src/db/types.ts`: Task 接口新增 `rectified_count: number` 字段
- `src/db/generator.ts`: 所有5处任务生成点均添加 `rectified_count`（约为隐患数的65%）

### 页面变更 (SpecialDimension.tsx)
- 删除表格"状态"列（thead + tbody）
- 在"重大隐患数"右侧新增"已整改"列（可排序）
- 修复 colSpan 从12改为11
- 移除了不再使用的 `statusStyle` 变量

### 修改记录弹窗
- 在 tab 按钮左侧新增 📝 按钮
- 点击后弹出修改记录弹窗，显示：
  - 修改时间（2026-07-01 14:33）
  - 修改内容（删除状态列 + 新增已整改列）
  - 后续每次修改需要在此弹窗追加记录

## 修改: 调整 📝 按钮位置到上方 "任务计划" tab 右侧

- 将 📝 修改记录按钮从 SpecialDimension 下方子 tab 行（全部/日常检查/专项检查/督查督办/抽检）的左侧，移到上方主 tab 栏 "任务计划" 的右侧
- 相关 state 和弹窗 JSX 从 SpecialDimension 移入 StationChiefV2Dashboard
- 弹窗仅在 "任务计划" tab 被选中时显示 📝 按钮

## 修改: 指标卡跟随子 tab 维度变化 + 新增3个隐患指标

- `stats` useMemo 改为使用 `currentTasks`（当前 tab 过滤后的数据），替代之前的 `filteredTasks`
- 新增3个隐患指标卡：隐患总数（橙色）、重大事故隐患数（红色）、已整改隐患数（绿色）
- 指标卡从3个扩展为6个，2行×3列网格布局
- 修改记录弹窗追加 2026-07-01 17:15 条目

## 修改: 6个指标合并为一行、统一UI风格

- 布局从 2行×3列 grid 改为 flex 单行，6个卡片等宽排列
- 统一白色背景 + 灰色边框，左侧用彩色 accent bar 区分
- 数值颜色统一为 `#111827`，仅"已完成"绿色、"重大事故隐患数"红色保留特殊
- 字体尺寸统一缩小（标题11px、数值22px），间距统一
