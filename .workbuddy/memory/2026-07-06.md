# 2026-07-06

## 全局筛选新增"责任主体类型"

- 在风险等级筛选右侧新增"主体"筛选器，包含三个选项：全部 / 生产企业 / 消防场所
- 风格与风险筛选按钮一致（选中态 #EEF2FF 底 + #4F46E5 文字）
- 筛选状态 `filterEntityType` 已传递给所有子 tab 组件（Duty/Industry/Special/State/Hazard/Trend）
- 重置按钮联动：当主体筛选非 all 时也显示重置按钮

### 修改文件
- `src/business/emergency/pages/StationChiefV2Dashboard.tsx`: 新增 state + 筛选 UI + 传递 prop
- `src/business/emergency/pages/components/types.ts`: 新增 EntityType，各 DimensionProps 增加 filterEntityType
- `src/business/emergency/pages/components/TrendDimension.tsx`: TrendDimensionProps 增加 filterEntityType

## 组织与人员tab - "所在工作组"列增加排序功能

- 人员履职情况表中"所在工作组"列改为可排序（SortableTh），sortKey 为 `work_groups`
- `useSortableTable.ts` 增加数组类型排序支持：对数组字段按 `join('、')` 后的字符串进行 localeCompare 比较
- `DutyDimension.tsx`: `<th style={thStyle}>所在工作组</th>` → `<SortableTh label="所在工作组" sortKey="work_groups" .../>`

## 企业老板看板 - 新增安全责任主体情况 & 重新编号

- 新增 "一、安全责任主体情况" 模块（仅当 `hasTenant===true` 时显示，有承租单位的版本才展示）
  - 企业信息概览：企业名称、承租企业数量、场所/厂区数量
  - 承租企业风险分级管控统计：重大/较大/一般/低风险卡片
  - 承租单位明细表：序号、单位名称、各级风险数、人员数量、安全管理人员、安全管理状态
- Mock 数据：`enterprise-boss.ts` 新增 `safetyResponsibility` 字段（含 `hasTenant` 控制开关）
- 重新编号所有 section：安全责任主体→一、风险分级→二、隐患排查→三、教育培训→四、现场管理→五、制度台账→六（移到底部）

### 修改文件
- `src/business/emergency/pages/EnterpriseBossDashboard.tsx`: 新增 section + 重编号 + 制度台账移到底部
- `src/business/emergency/pages/mock/enterprise-boss.ts`: 新增 `safetyResponsibility` mock 数据
