# 2026-08-12 工作记录

## 企业覆盖柱状图顺序 - 推送完成
- 用户确认推送。本地 headless 实测当前代码（JSX=[checkEnt,safeEnt,hazardEnt]）初始+切换后均为 紫-检查→绿-无隐患→橙-有隐患，切换稳定。
- 推送 `ad05ada`（临时仓库流程：tar 复制文件 + alt-index add + write-tree/commit-tree/update-ref），GitHub Actions 部署完成。
- **线上 playwright 实测顺序 = 紫→绿→橙 ✓**。最终定论：recharts v3.8.1 Bar 顺序 = JSX 声明顺序，无旋转（详见 08-11 记录）。

## 新增「查宣一体」顶级页签（AI眼镜检查看板右侧）
- 新建 `src/business/emergency/pages/ChaxuanYitiDashboard.tsx`，路由 `page=chaxuan-yiti`。
- **置顶时间筛选**：本周/上周/本月/上月 + 自定义日期（复用 AiGlassesDashboard 逻辑，sticky 吸顶）。
- **模块1 宣教推送与学习**：12 个 KPI（推送人数/次数/人均/点击率、点击人数/次数、播放次数/时长/人均/进度、积分发放/消耗），每个带**周环比**（红涨绿跌）。
- **模块2 每周变化趋势图**：按 推送/点击/积分 三维度切换折线图（条件渲染 Line，已验证切换正确）。
- **模块3 隐患整改**：检查次数、发现隐患数|已整改|整改完成率、重大事故隐患数|已整改|整改完成率，均带周环比。
- **模块4 隐患每周趋势图**：检查次数、发现隐患数、整改完成率（右轴%）折线图。
- 折线图用 LineChart（非 Bar，无顺序问题），自定义 TrendTooltip/TrendLegend 统一顺序。
- 验证：playwright 实测页面渲染正常、维度切换正确（推送2条/点击3条/积分2条 + 隐患图3条）、无 JS 错误。
- **未推送**（用户要求"我叫你推送时才推送"），待用户确认后走临时仓库流程推送。

## 查宣一体 - 模块1 分组框调整
- 按用户规则重构宣教推送与学习：**`|` 分割的指标放同一框、`、` 分割的分成不同框**。
- 现为 3 个大框（GroupKpiCard：标题 + 内部 grid 均分指标列，每列 label/value/周环比）：
  1. 推送：推送人数/推送次数/人均推送次数/推送点击率
  2. 点击播放：点击人数/点击次数/播放次数/播放时长/人均播放时长/平均播放进度
  3. 积分：积分发放数/积分消耗数
- playwright 实测渲染正确，无 JS 错误。尚未推送。

## 查宣一体 - UI 间距优化
- 反馈"指标内容挤在一起"：加大卡片内外留白。
- cardBase: padding '10px 12px' → '14px 16px'
- GroupKpiCard: padding '12px 14px' → '16px 18px'、标题加粗放大、框内 gap 8 → 16、指标列 label/value/环比间距加大
- KpiCard/TripleCard: label/value/mom 间距加大（marginBottom 3→8、marginTop 2→6）、字号略增
- WeeklyMom: marginTop 2 → 6
- 容器 grid gap 10/12 → 16、minmax 240/320 → 300/460
- playwright 截图确认：推送框(4指标)、点击播放框(6指标)、积分框(2指标) 内部不再拥挤。尚未推送。

## 查宣一体 - 隐患整改改为并排
- 用户反馈：隐患整改的 `|` 分割指标被堆叠了，应平铺并排。
- TripleCard（堆叠：主值/已整改/完成率分行）改为 **RowCard**（一个框内 grid 多列并排，每列 label+value+周环比）。
- 隐患整改现为 3 个框：检查次数(单卡) / 发现隐患数|已整改|整改完成率 / 重大事故隐患数|已整改|整改完成率，每列均带周环比。
- 坐标实测：发现隐患数(x508)、已整改(x641)、整改完成率(x775) 同一 y=1174 行并排 ✓。尚未推送。

## 查宣一体 - 模块序号 + 宣教3框同行 + 宽度自适应
- 4 个模块 SectionBlock 标题加序号：1、宣教推送与学习 / 2、每周变化趋势 / 3、隐患整改 / 4、隐患每周变化趋势。
- 宣教模块 3 框改为**同一行**：gridTemplateColumns 按指标数比例 `4fr 6fr 2fr`（自动生成），积分框(2指标)宽度收窄到 181px（vs 推送 391 / 点击播放 602）。
- GroupKpiCard 字体调小：label 12→11px、value 18→16px、框内 gap 16→10、padding 16→12；label 加 ellipsis 防溢出。
- playwright 实测：4 个序号标题 ✓、三框同 y=359 行、宽度 391/602/181 ✓。尚未推送。

## 查宣一体 - 隐患整改样式同步
- 隐患整改模块与宣教模块样式对齐：grid `1fr 3fr 3fr`（按指标数比例 1:3:3，同一行），实测宽度 181/542/542。
- **所有指标数值字体统一黑色 #111827**（移除发现隐患数红色/已整改绿色等 color 参数）。
- 周环比样式保持不变（WeeklyMom 红涨绿跌）。
- RowCard 样式对齐 GroupKpiCard：label 11px、value 16px、gap 10、padding 12px 14px。尚未推送。

## 查宣一体 - 字体统一
- 用户反馈"检查次数字体比其他大"：KpiCard 数值 20px → **16px**、label 12 → 11px，与 GroupKpiCard/RowCard 完全统一。
- playwright 实测：检查次数 1,750 / 发现隐患 98 / 已整改 79 / 重大隐患 12 / 推送人数 12,864 等全部 16px 黑色 #111827 ✓。尚未推送。

## 查宣一体 - 模块2 标题+副指标
- 模块2 标题改为「2、每周变化趋势」（去掉维度说明括号）。
- 推送维度新增**推送点击率**(pushRate, 紫 #8B5CF6, 虚线)；点击维度新增**人均播放时长**(avgPlayTime, 青 #14B8A6, 虚线)。
- WEEKLY_TREND 每行加 pushRate/avgPlayTime 字段；Tooltip 单位处理（pushRate 加 %、avgPlayTime 加 分钟）。
- playwright 实测：标题 ✓、推送 3 线/点击 4 线（+隐患图 3 线恒在）✓、图例含新指标 ✓。尚未推送。

## 查宣一体 - 模块2 副指标双Y轴
- pushRate（推送点击率 %）、avgPlayTime（人均播放时长 分钟）改用 **yAxisId="right"** 右轴，主指标用 yAxisId="left"。
- 模块2 LineChart 增加右 YAxis（orientation="right"）。
- playwright 实测：左轴刻度 0/6000/12000/18000/24000（主指标）、右轴刻度 0/20/40/60/80（副指标 %）✓ 双轴生效。尚未推送。

## 查宣一体 - 模块4 指标+维度切换
- 模块4 指标改为：**发现隐患数 / 已整改 / 整改完成率**（去掉检查次数）。
- 新增维度切换按钮：**隐患总数 | 重大事故隐患数**（state hazardDim）：
  - 隐患总数：发现隐患数(left红) / 已整改(left绿) / 整改完成率(right黄%)
  - 重大事故隐患数：重大事故隐患数(left深红) / 已整改(left亮绿) / 整改完成率(right橙%)
- WEEKLY_TREND 每行加 rectified / majorHazard / majorRectified / majorRate 字段。
- playwright 实测：两维度各 3 条线、图例正确 ✓。尚未推送。

## 查宣一体 - 模块2 颜色区分 + 图例虚线
- 推送维度颜色区分：推送人数 #4F46E5(靛蓝) / 推送次数 #3B82F6→**#06B6D4(青)** / 推送点击率 #8B5CF6→**#A855F7(紫，虚线)**。
- SERIES_META 增加 `dashed` 标记（pushRate、avgPlayTime=true）；TrendLegend 改用 SVG line 渲染，dashed 项显示虚线（stroke-dasharray="4 2"），与图中线型一致。
- 点击维度人均播放时长图例也显示虚线。
- playwright 实测：推送图例 蓝/青/紫(虚线)、点击图例 绿/蓝/橙/青(虚线) ✓。尚未推送。

## 查宣一体 - 时间轴改「几月第几周」
- WEEKLY_TREND 的 week 字段从 W27-W34 改为中文「7月第1周…7月第5周、8月第1周…8月第3周」。
- 模块2/模块4 共用数据，X 轴与 Tooltip 自动同步显示中文周格式（playwright 实测两图均正确）。尚未推送。

## 查宣一体 - 图例虚线长度修复
- 用户反馈推送点击率图例看似实线：SVG line width 仅 18px + dash "4 2" 间隔太小，肉眼难以辨认为虚线。
- 改为 width 32px + dash "6 3"，虚线明显（3 段 dash），与图中虚线一致。点击维度人均播放时长图例同样生效。
- playwright 截图确认推送点击率图例明显虚线 ✓。尚未推送。

## 查宣一体 - 模块2/4 标题加「近两月」
- 模块2 标题 →「2、每周变化趋势（近两月）」；模块4 标题 →「4、隐患每周变化趋势（近两月）」。尚未推送。

## 查宣一体 - 推送完成（2026-08-12 14:08）
- 提交 `a12beff`（基于 ad05ada）：新增 ChaxuanYitiDashboard.tsx + StationChiefV2Dashboard 加 tab + memory 文件。
- 推送 `ad05ada..a12beff master -> master`，GitHub Actions 部署完成。
- 线上 playwright 验证：4 个模块标题（含"近两月"）✓、内容（推送人数 12,864 / 7月第1周 / 重大事故隐患数）✓。

## 镇街数据看板 - 各Tab介绍文档
- 新建 `docs/镇街数据看板-tab介绍.md`：为 station-chief-v2 下 6 个顶级 tab（应急消防管理站看板/日常监管/村社数据看板/三方同步任务看板/AI眼镜检查看板/查宣一体）各写一段介绍（用途+关键数据）。
- 依据：StationChiefV2Dashboard 分支结构（station→多维度、daily→DailySupervisionDimension、yuzhi→YuzhiSyncDimension、yuzhi-sync→YuzhiSyncDashboard、ai-glasses、chaxuan-yiti）。尚未推送。

## 查宣一体 - 新增「宣教明细数据」表格模块（2026-08-13）
- 在模块2（每周变化趋势）与模块3（隐患整改）之间新增「宣教明细数据」表格。
- 列：序号/宣教内容名称/推送人数/推送次数/推送点击率/点击人数/点击次数/播放次数/播放时长/人均播放时长/平均播放进度。
- mock 数据 PROMOTION_DETAIL（8 条宣教内容），表头灰底、斑马纹、横向滚动（minWidth 1080）。
- playwright 实测：8 行 × 11 列渲染正确。尚未推送。

## 查宣一体 - 宣教明细模块加序号+列排序（2026-08-13）
- 模块标题加序号「3、宣教明细数据」，后续模块顺延：隐患整改→4、隐患每周变化趋势→5。
- 表格每列可排序：DETAIL_COLS 列配置 + sortKey/sortDir 状态，点击表头 升序↔降序 切换，序号列随排序重排 1-N，名称列用 localeCompare（中文）。
- 表头排序图标：未排↕ / 升▲ / 降▼，激活列高亮蓝色。
- playwright 实测：推送人数升降序、名称排序均正确。尚未推送。

## 推送完成 + 源仓库解锁新流程（2026-08-13 16:33）
- 提交 `3014ae7`（宣教明细表格+排序、docs 介绍），推送 `a12beff..3014ae7`，部署完成。
- **重要变化**：源仓库 .git 锁已释放！git status/fetch/reset 可用；但 `.git/index.lock`（Aug 11 遗留 0 字节）仍被宿主锁、safe-delete 拦截删除。
- **新推送流程（无需临时仓库）**：`git fetch xjh` → `git reset --soft xjh/master`（对齐远端 HEAD）→ `export GIT_INDEX_FILE=<proj>/.git/alt-indexN` + `git add -A` → `git write-tree` → `git commit-tree $TREE -p xjh/master` → `git update-ref refs/heads/master $COMMIT` → `git push xjh master`。update-ref 已不再被锁。
- 线上 playwright 验证：模块标题 1-5 ✓、宣教明细 8 行 ✓、10 个可排序表头 ✓。
